apiVersion: v1
kind: Service
metadata:
  name: {{ include "endgame.fullname" . }}
  labels:
    {{- include "endgame.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "endgame.selectorLabels" . | nindent 4 }}
