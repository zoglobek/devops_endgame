apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "endgame.fullname" . }}-test-connection"
  labels:
    {{- include "endgame.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "endgame.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
